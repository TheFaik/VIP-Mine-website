{
  "scripts": {
    "dev": "vitepress dev",
    "predeply": "npm run build",
    "deploy": "gh-pages -d dist",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vitepress": "^1.6.3"
  }
}
